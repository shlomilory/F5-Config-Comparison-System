# F5 Configuration Comparison System - Lambda Dependencies
# Python 3.11

# SSH/SFTP client for F5 connectivity
paramiko==3.4.0

# AWS SDK for Python (usually pre-installed in Lambda, but specified for completeness)
boto3>=1.34.0
botocore>=1.34.0

# Paramiko dependencies (usually installed automatically, but listed for clarity)
bcrypt>=4.0.1
cryptography>=41.0.0
pynacl>=1.5.0